# minikube

```
$ minikube start
$ minikube start --cpus 4 --memory 4096
$ minikube stop
$ minikube dashboard
$ minikube delete
$ minikube ip
$ minikube ssh
$ minikube config set memory 4096
$ minikube config set cpus 2
```
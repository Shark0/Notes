apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: nginx-ingress
spec:
  rules:
    - host: shark.nginx.com
      http:
        paths:
          - backend:
              serviceName: nginx-service
              servicePort: 80